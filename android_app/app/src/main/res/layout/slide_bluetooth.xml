<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Slide 5 de 7: parear a balança.
  ~ Mantém os ids btnSearchScale e txtFoundDevice, usados por
  ~ BluetoothIntroSlide para disparar a busca e mostrar o resultado.
  -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/android:colorBackground"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingBottom="120dp">

        <ImageView
            android:id="@+id/imgSlide"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginTop="72dp"
            android:background="@drawable/bg_rd_slide_icon"
            android:contentDescription="@null"
            android:padding="24dp"
            app:srcCompat="@drawable/ic_rd_scale"
            app:tint="?attr/colorOnPrimaryContainer" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:gravity="center"
            android:text="@string/label_slide_bluetooth_top_text"
            android:textAppearance="@style/TextAppearance.OpenScale.ScreenTitle" />

        <TextView
            android:id="@+id/slideMainText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.15"
            android:text="@string/label_slide_bluetooth_main_text"
            android:textAppearance="@style/TextAppearance.OpenScale.Body"
            android:textColorLink="?attr/colorPrimary" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSearchScale"
            style="@style/Widget.OpenScale.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="@string/rd_slide_add_scale" />

        <!-- Resultado da busca. Fica vazio até o usuário procurar. -->
        <TextView
            android:id="@+id/txtFoundDevice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.OpenScale.Label"
            android:textColor="?attr/colorOnSurface"
            tools:text="Beurer BF700" />
    </LinearLayout>
</ScrollView>
